import '../../entities/advantage.dart';

// Para excluir as mudanças ao arquivo, este arquivo pode ser marcado como "sem mudanças" com o comando
// `git update-index --assume-unchanged`. Assim, mesmo ao alterar o arquivo, o git irá ignorar as mudanças que neste,
// neste arquivo, por exemplo, não devem ser submetidas por conta de licenças de distribuição do manual de RPG 3DeT
// Victory. Para reverter isto, use o comando `git update-index --no-assume-unchanged`.

class DefaultAdvantages {
  static final Set<Advantage> defaultAdvantages = <Advantage>{
    // hidden licensed information
  };

  // hidden licensed information
}
